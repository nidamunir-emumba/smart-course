"""SQLAlchemy ORM models (PostgreSQL source of truth).

Define here and import in migrations/env.py for autogenerate. Core entities:
User, Course, Module, Asset, Enrollment, Progress, Certificate.
Kept as a stub for the infra scaffold — add models, then `make revision`.
"""
