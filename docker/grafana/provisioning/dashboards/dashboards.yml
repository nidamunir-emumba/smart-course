apiVersion: 1

# Drop dashboard JSON files into this folder and they are auto-loaded.
providers:
  - name: SmartCourse
    orgId: 1
    folder: SmartCourse
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
